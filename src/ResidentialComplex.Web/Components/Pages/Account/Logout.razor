@page "/Account/Logout"

@code {
    // Logout is handled by the GET endpoint at /Account/Logout in Program.cs.
    // This page exists only as a route placeholder; actual logout is via the endpoint.
}
